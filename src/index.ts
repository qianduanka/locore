/*
 * @Author: qianduanka
 * @Date: 2024-03-10
 * @Description:常用方法 locore
 * @information：微信号 qianduanka
 */
import { sort, max, min, unique } from './array';
import { delString, insertString } from './string';
import { debounce, throttle } from './function';
import { deepClone, extend } from './object';
import { type, isPrimitive } from './type';

export {
    sort,
    max,
    min,
    unique,
    debounce,
    throttle,
    deepClone,
    extend,
    delString,
    insertString,
    type,
    isPrimitive
}